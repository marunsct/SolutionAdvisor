name: Copilot SWE Agent CI

on:
  push:
    branches: [ main, develop, 'copilot/**', 'fix/**' ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Kill process on port 4004
      run: |
        if command -v lsof >/dev/null 2>&1; then
          lsof -ti:4004 | xargs -r kill || true
        else
          ss -ltnp 2>/dev/null | awk '/:4004/ {print $6}' | xargs -r kill || true
        fi
    
    - name: Build CDS model
      run: npx cds build
    
    - name: Start server and run tests
      run: |
        npm run start &
        SERVER_PID=$!
        sleep 5
        # Add your test commands here
        # npm test
        kill $SERVER_PID || true
